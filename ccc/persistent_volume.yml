apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cloudcomp-solaiman
spec:
  storageClassName: rook-cephfs-central	
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
